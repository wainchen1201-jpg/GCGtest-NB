# 三裝置試點 SOP

照著做就好。每一格都有「指令 → 預期看到什麼 → 沒看到怎麼辦」。

**三台機器用 A / B / C 稱呼。** 開始前先決定哪台是 A、哪台是 B、哪台是 C，寫下來，
整份 SOP 都用這個代號，不要中途改。

**每一步都要記 exit code。** PowerShell 裡跑完立刻打 `$LASTEXITCODE`。
這份系統大量使用 exit 2（「停下來了，需要你判斷」，**不是錯誤**），
不記下來就分不出「它擋我」跟「它壞了」。

```
exit 0 = 完成
exit 1 = 失敗（程式或環境問題）
exit 2 = 停下來了，需要你判斷 ← 很多時候這才是正確結果
```

---

## 用語

| 寫法 | 意思 |
|---|---|
| `<專案>` | 測試專案資料夾，例如 `D:\試點A`。**這個資料夾就是開工包改名而來的** |
| `<開工包>` | 解壓縮出來、還沒改名的那份，只有第 0 階段用得到 |
| `_bootstrap\` | 開工包自帶的腳本。**初始化之前**用這裡的 |
| `.hybrid\scripts\` | 初始化之後專案自帶的腳本。**之後一律用這裡的** |
| `PS>` | 一般 PowerShell 視窗 |
| `PS#>` | **系統管理員** PowerShell 視窗 |

執行腳本一律用這個形狀（避免執行原則擋下來）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "<腳本路徑>" <參數>
```

---

# 第 0 階段：三台都要做的準備

三台機器**各自**做完 0-1 到 0-4 才能往下。順序無所謂，但三台都要做完。

## 0-1　把開工包解壓縮

| | |
|---|---|
| **在哪** | A、B、C 各一次 |
| **做什麼** | 把 `開工包.zip` 複製到這台機器，解壓縮 |
| **預期** | 得到一個資料夾，裡面**直接**看到 `開工.cmd`、`試點SOP.md`、`_bootstrap\` |
| **失敗怎麼辦** | 如果變成 `開工包\開工包\開工.cmd`（多一層），把裡面那層搬出來 |

**不要用 `git pull` 拿。** 這一版還沒推上 GitHub，模板 repo 裡是舊的，
照著跑會驗到錯的程式碼。**這個 zip 才是要測的版本。**

B、C 兩台解出來先放著就好（0-2 要用它）。A 機的那份會在 1-1 改名成專案。

## 0-2　安裝排程與 runtime（**需要提權，一台一次**）

| | |
|---|---|
| **在哪** | A、B、C 各一次 |
| **指令** | `PS#> powershell -NoProfile -ExecutionPolicy Bypass -File "<開工包>\_bootstrap\install-heartbeat.ps1"` |
| **預期** | exit 0，訊息說排程項目已註冊 |
| **失敗怎麼辦** | 出現 `Access is denied` 代表沒有用系統管理員身分開視窗。關掉重開，右鍵「以系統管理員身分執行」 |

**這是整個試點唯一需要提權的一步。** 之後所有動作都不需要。

## 0-3　確認裝好了

| | |
|---|---|
| **在哪** | A、B、C 各一次 |
| **指令** | `PS> powershell -NoProfile -ExecutionPolicy Bypass -File "<開工包>\_bootstrap\runtime-status.ps1"` |
| **預期** | 印出「這台機器的 runtime／版本／schema：認得 1–2」 |
| **失敗怎麼辦** | 印不出版本就是 0-2 沒有真的裝好。**先停，不要往下做**——後面每一項都建立在「三台跑同一版 runtime」這個前提上 |

## 0-4　確認裝置身分的狀態

| | |
|---|---|
| **在哪** | A、B、C 各一次 |
| **指令** | 同 0-3，看輸出裡「這台機器的裝置身分」那一段 |
| **預期** | `deviceId ：還沒產生（第一次開工時才會鑄造）` |
| **失敗怎麼辦** | 印出 `讀不動` → 那個檔案存在但壞了。**先停**，把整段貼給我——開工不會重鑄一顆，會直接停下來 |

**「還沒產生」在這個階段是正確答案，不是問題。** `device.json` 是第一次開工時才鑄造的，
`install-heartbeat` 不會產生它。三台的值要等到各自開過工才比得了——排在第 2 階段開頭。

如果這裡就印出了一顆 `deviceId`，代表這台機器以前跑過這套系統。那也沒關係，
照樣往下做，只是 2-0 的比對要特別留意它跟另外兩台是不是同一顆。

---

# 第 1 階段：建測試專案（只在 A 做）

**不要拿正在用的專案試。** 後面會刻意斷網、竄改檔案、刪素材。

> 這一節的順序是實跑驗過的，**不要跳步**。初始化不會做任何 commit，`_drive/` 要開工
> 之後才存在——這兩件事各自決定了下面某一步為什麼一定要排在那個位置。

## 1-1　建專案資料夾並初始化

| | |
|---|---|
| **在哪** | **只有 A** |
| **做什麼** | 再解壓一份開工包，**把資料夾改名成 `試點A`**（這個資料夾本身就是專案，不是安裝程式） |
| **指令** | `PS> powershell -NoProfile -ExecutionPolicy Bypass -File "D:\試點A\_bootstrap\initialise.ps1" -ProjectRoot "D:\試點A"` |
| **預期** | exit 0，印「初始化完成」，且 `自帶工具：腳本與 skill 已複製進 .hybrid/scripts 與 .claude/skills` |
| **失敗怎麼辦** | 找不到 Google Drive 掛載點 → 加參數 `-DriveRoot "H:\我的雲端硬碟"`（換成你實際的路徑） |

**「自帶工具」那一行一定要說「已複製」。** 說「沒有複製」就是壞了——後面每一步都要用
`.hybrid\scripts\` 底下的腳本。

**把專案 ID 抄下來**（形如 `a-20260902-1a2b3c4d`）。純中文名的專案，slug 會退化成
`project-` 或單一字母——這是已知行為，靠後面的 UUID 區分，不是缺陷。

## 1-2　做出第一個 commit

| | |
|---|---|
| **在哪** | **只有 A** |
| **指令** | `PS> git -C "D:\試點A" add -A`<br>`PS> git -C "D:\試點A" commit -m "chore: 初始化專案骨架"` |
| **預期** | 產生一筆 commit |
| **失敗怎麼辦** | `Author identity unknown` → 這台機器的 git 還沒設姓名信箱，先設好再重跑 |

**這一步不能省。** `initialise` 只做 `git init`，**不做任何 commit**；第一個 commit
原本要等到收工才產生。在那之前 `master` 這個分支根本不存在，下一步推送會得到
`src refspec master does not match any`——那不是打錯字，是真的沒有東西可推。

## 1-3　接上 GitHub

| | |
|---|---|
| **在哪** | **只有 A** |
| **做什麼** | 在 GitHub 開一個**私有** repo（**不要**勾選 README／.gitignore，要空的） |
| **指令** | `PS> git -C "D:\試點A" remote add origin <你的 repo 網址>`<br>`PS> git -C "D:\試點A" push -u origin master` |
| **預期** | `[new branch] master -> master` |
| **失敗怎麼辦** | 認證失敗 → 先把這台機器的 GitHub 認證設好（`git push` 會跳登入）。這一步做不成，後面的交接全部無法進行 |

## 1-4　第一次開工

| | |
|---|---|
| **在哪** | **只有 A** |
| **指令** | `PS> powershell -NoProfile -ExecutionPolicy Bypass -File "D:\試點A\.hybrid\scripts\startup.ps1" -ProjectRoot "D:\試點A"` |
| **預期** | exit 0，印「開工完成」，且 `_drive/：已建立`、`租約：已取得`、`主線：master（已經是最新的）` |
| **失敗怎麼辦** | exit 2 →**看訊息**，它會說明是哪一種情況。把整段輸出貼給我 |

**注意這裡改用 `.hybrid\scripts\`**，不是 `_bootstrap\`。初始化之後專案自帶腳本，
之後一律用自帶那份。

## 1-5　放一個真的大素材

| | |
|---|---|
| **在哪** | **只有 A** |
| **做什麼** | 開檔案總管，把一個 **50 MiB 以上**的真實檔案（KiCad 3D 模型、廠商 PDF）放進 `D:\試點A\_drive\素材\` |
| **預期** | 檔案放得進去。它其實是寫到 Google Drive 上 |
| **失敗怎麼辦** | `_drive` 資料夾不存在 → 1-4 沒有成功，回去看那一步的輸出 |

**這一步一定要排在開工之後**：`_drive/` 是開工時才建立的 junction，初始化階段還不存在。

**為什麼一定要真的大檔**：preflight 的門檻（50 MiB 警告／100 MiB 阻擋）和 `drive-verify`
的雜湊比對，用小檔都驗不到真實情況。

## 1-6　再建 1–2 個專案

重複 1-1 到 1-5。其中一個刻意取**純中文名**（例如 `D:\電源板`），那會驗到專案 ID 的
slug 退化與碰撞處理。

---

# 第 2 階段：正常交接 ×10

## 2-0　先比對三台的 deviceId（做完第 1 次交接之後）

第 1 次交接（A→B）走完時 A、B 都開過工了；C 要等第 2 次交接之後。**三台都開過工，
就馬上做這件事，不要等到十次跑完。**

每一台跑 `runtime-status.ps1`，抄下「裝置身分」那一段的值：

```
A 的 deviceId ＝
B 的 deviceId ＝
C 的 deviceId ＝
```

**三個值必須互不相同。** 有任何兩個一樣就**停下來**，把三個值貼給我——那代表
`device.json` 是被複製過去的（整個使用者資料夾被搬移或還原過），租約的所有權判定
會整批失真：兩台機器會互相認成「自己」，忘記收工那一項根本擋不住。

這件事愈早發現愈好。等十次交接跑完才發現，前面的結果全部要重跑。

---

一次交接 ＝ **這台收工 → 下一台開工 → 確認拿得到進度**。

**必須連續 10 次成功。** 中間任何一次失敗，記下來、修好、**從第 1 次重新算**。

## 交接的標準動作

### 步驟 A：在「交出去」的那台

| 順序 | 指令 | 預期 |
|---|---|---|
| 1 | 改點東西（隨便編輯專案裡一個檔案） | — |
| 2 | 在 `<專案>\.hybrid\next.md` 寫 1–3 行「接下來要做什麼」 | — |
| 3 | `powershell ... -File "<專案>\.hybrid\scripts\shutdown.ps1" -ProjectRoot "<專案>"` | exit **2**，卡在「還沒確認 Google Drive 同步完成」 |
| 4 | 等 Google Drive 圖示顯示同步完成 | — |
| 5 | 同樣的指令加 `-DriveSynced` | exit **0**，印「結論：可以換裝置」 |

**第 3 步的 exit 2 是正常的**，不是失敗。Drive 的同步完成時點程式測不到，所以停下來問人。

而且**第 3 步其實已經把工作提交並推上去了**（輸出會有「本次變更：已壓成一筆併入 master」
與「主線：master（已推送）」），租約也已經釋放。第 5 步只是補上那一句確認，所以它跑得很快。
知道這件事的意義是：第 3 步之後你的工作已經安全了，不會因為忘了跑第 5 步而遺失。

### 步驟 B：在「接手」的那台

| 順序 | 指令 | 預期 |
|---|---|---|
| 1 | 第一次接手要先 clone：`git clone <repo 網址> "<專案>"` | — |
| 1b | **第一次接手**的開工可能要加 `-DriveRoot "<你的 Drive 路徑>"` | 見下方說明 |
| 2 | `powershell ... -File "<專案>\.hybrid\scripts\startup.ps1" -ProjectRoot "<專案>"` | exit **0**，印「開工完成」 |
| 3 | 看摘要的「上次進度」那一行 | **要對得上前一台做的事** |
| 4 | 看摘要的「接下來」那幾行 | **要是前一台寫進 `next.md` 的內容** |
| 5 | 看摘要的「心跳健康」那一行 | 正常時**只有一行** |

**關於 1b**：記住掛載點的 `.hybrid\local.json` **不進版控**（每台裝置各自持有），
所以新 clone 下來沒有它，接手的機器要自己解析 Drive 位置。自動偵測找得到就不必加參數；
找不到或指到錯的地方，會 exit 2 並要你用 `-DriveRoot` 指定。指定過一次就會被記住。

## 10 次的順序與各自要多看的東西

| # | 從 | 到 | 這次多驗 |
|---|---|---|---|
| 1 | A | B | 基本路徑走得通 |
| 2 | B | C | 第三台第一次接手 |
| 3 | C | A | 繞回起點，A 的舊租約沒有殘留 |
| 4 | A | B | 「上次進度」對得上 C 做的事 |
| 5 | B | A | 收工後 **A 連開兩次工**——第二次應印「續用」 |
| 6 | A | C | 跳過 B |
| 7 | C | B | `next.md` 寫**四行**，確認只顯示前三行 |
| 8 | B | C | 用有大素材的那個專案 |
| 9 | C | A | **等心跳跑過一輪**（15 分鐘）再收工 |
| 10 | A | B | 收工後**隔一天**再開工 |

### 記錄表

每次填一列：

| # | 從→到 | 收工 exit | 開工 exit | 上次進度對嗎 | next.md 對嗎 | 備註 |
|---|---|---|---|---|---|---|
| 1 | | | | | | |
| 2 | | | | | | |
| … | | | | | | |

---

# 第 3 階段：故障演練 ×7

**每一種都要真的造出那個狀況。** 每一項做完把證據存成一個檔案。

## 3-1　忘記收工（最重要的一項）

這是這次改動裡**你日常感受最明顯的變化**。

| 順序 | 在哪 | 做什麼 | 預期 |
|---|---|---|---|
| 1 | A | 開工，改點東西，**不收工** | exit 0 |
| 2 | A | `Get-Content "<專案>\_drive\lease.json"` 存成 `3-1-租約-事前.txt` | 看到 A 持有 |
| 3 | B | 開工 | **exit 2**，訊息說 A 持有租約，印出「判定：」那一行，並列出可以走的路 |
| 4 | B | 檢查 `<專案>\_drive` 資料夾在不在 | **要在**（診斷狀態刻意留著） |
| 5 | B | `Get-Content "<專案>\_drive\lease.json"` 存成 `3-1-租約-事後.txt` | **要跟事前一模一樣** |
| 6 | B | `powershell ... -File "<專案>\.hybrid\scripts\proxy-shutdown.ps1" -ProjectRoot "<專案>" -Confirmed` | exit 0，拿回 A 的進度 |
| 7 | B | 再開工 | exit 0 |

訊息裡的「判定」會是 `active`、`stale` 或 `unknown` 其中之一，**三種都算正常**，
差別只在它接著建議什麼。心跳從沒推上來過時會是 `unknown`，那時只剩「明確覆寫」與
「代理收工」兩條路——**路的條數不固定，不要拿條數當驗收標準**，要看的是它有沒有
擋下來、有沒有說清楚為什麼。

**失敗怎麼辦**：
- 第 3 步 exit 0（沒被擋）→ **這是嚴重問題**，立刻記下來停止這一項，貼給我
- 第 5 步兩份檔案不一樣 → **嚴重問題**，工具動了別台的租約

## 3-2　離線／認證失敗

| 順序 | 在哪 | 做什麼 | 預期 |
|---|---|---|---|
| 1 | 任一台 | 關掉 Wi-Fi / 拔網路線 | — |
| 2 | 同一台 | 開工 | **exit 2**，訊息並列「認證／網路／repo 不存在」三種可能 |
| 3 | 同一台 | 檢查有沒有印「開工完成」 | **不該有** |
| 4 | 同一台 | 檢查 `_drive\lease.json` | **不該產生新租約** |
| 5 | 同一台 | 恢復網路，再開工 | exit 0 |

**失敗怎麼辦**：第 2 步如果 exit 0 並說「已經是最新的」→ **嚴重問題**。這正是這次修掉的
一個洞（離線時開工照樣取得租約，讓你在落後的主線上工作一整天）。

## 3-3　遠端分岔

| 順序 | 在哪 | 做什麼 | 預期 |
|---|---|---|---|
| 1 | A | 開工、改東西、`git commit`，**但斷網讓推送失敗** | — |
| 2 | B | 正常開工、改東西、收工（推送成功） | exit 0 |
| 3 | A | 恢復網路，開工 | **exit 2**，訊息說明分岔與處理方式 |
| 4 | A | 照訊息處理（`git pull --rebase` 或決定那筆要不要留） | — |
| 5 | A | 再開工 | exit 0 |

## 3-4　Drive 還沒同步下來

| 順序 | 在哪 | 做什麼 | 預期 |
|---|---|---|---|
| 1 | B | 在 Google Drive 資料夾裡，把 `_hybrid\<專案 ID>` **改名**（例如加 `-暫存`） | — |
| 2 | B | 開工 | **exit 2**，訊息說「可能還沒同步完成」 |
| 3 | B | 檢查 Drive 端**有沒有多出一個空的** `_hybrid\<專案 ID>` 資料夾 | **不該有** |
| 4 | B | 改回原名，開工 | exit 0 |

**第 3 步是重點**。以前工具會補建一個空殼，那個空殼會同步上雲端、再散到另外兩台，
看起來就像「這個專案在 Drive 上本來就是空的」。

## 3-5　敏感檔案

| 順序 | 在哪 | 做什麼 | 預期 |
|---|---|---|---|
| 1 | 任一台 | 在專案根目錄建 `.env`，內容寫 `API_KEY=test123` | — |
| 2 | 同一台 | 收工（帶 `-DriveSynced`） | **exit 2**，指名 `.env` |
| 3 | 同一台 | 等 15 分鐘讓心跳跑一輪 | — |
| 4 | 同一台 | `Get-Content "<專案>\.hybrid\preflight-skip-log.jsonl"` | **一筆**紀錄，含 `.env` |
| 5 | 同一台 | 再等 15 分鐘 | — |
| 6 | 同一台 | 再看同一個檔案 | **仍然只有一筆**，`count` 變成 2 |
| 7 | 同一台 | 刪掉 `.env`，收工 | exit 0 |

**第 6 步是重點**：同一個問題不該每 15 分鐘記一行（一天 96 行）。

**第 3–6 步跟 3-6 一樣，需要心跳真的跑過。** 沒裝 runtime 的話派工器會印
「本輪不處理任何專案」，那個 jsonl 永遠不會出現——那不是缺陷，是這一項沒驗到。
第 4 步找不到檔案時先回頭確認 0-3 印得出版本。

## 3-6　竄改自帶腳本（信任邊界）

| 順序 | 在哪 | 做什麼 | 預期 |
|---|---|---|---|
| 1 | 任一台 | 把 `<專案>\.hybrid\scripts\heartbeat.ps1` 內容整個換成：<br>`param([string]$ProjectRoot)`<br>`New-Item "$ProjectRoot\我被執行了.txt" -Force` | — |
| 2 | 同一台 | 等 15 分鐘，或手動跑 `powershell ... -File "<開工包>\_bootstrap\run-heartbeats.ps1"` | — |
| | | **這一行一定要用 `_bootstrap` 那份。** 這一項驗的就是「排程跑的不是專案裡的程式」——拿專案自帶的派工器來跑，等於把要驗的東西先拆掉 | |
| 3 | 同一台 | 檢查 `<專案>\我被執行了.txt` 在不在 | **不該存在** |
| 4 | 同一台 | `git -C "<專案>" checkout -- .hybrid/scripts/heartbeat.ps1` | 還原 |

**先確認這一項真的驗到了東西。** 第 2 步的輸出如果出現：

```
runtime：這台機器還沒安裝 runtime（...current.json 不存在）
本輪不處理任何專案。
```

那就是**因為根本沒跑到而通過**，比失敗更危險——它會讓你以為信任邊界驗過了。
回頭確認 0-2 的提權安裝真的成功（0-3 要印得出版本），再重做這一項。

要看到的是心跳**確實跑了**（處理了這個專案），而哨兵檔**仍然沒有出現**。

**失敗怎麼辦**：第 3 步如果那個檔案出現了 → **最嚴重的問題**，代表排程仍在執行 repo 裡的
程式碼。立刻停止整個試點，貼給我。

## 3-7　睡眠與時鐘偏差

| 順序 | 在哪 | 做什麼 | 預期 |
|---|---|---|---|
| 1 | A | 開工後直接闔上筆電 | — |
| 2 | — | **隔一天** | — |
| 3 | B | 開工 | **exit 2**，訊息裡有「判定：」那一行 |
| 4 | B | 記下判定是 `active`、`stale` 還是 `unknown` | 三種都算正常 |

這一項**沒有標準答案**，重點是記下實際看到什麼。

---

# 第 4 階段：還原演練 ×1

用有大素材的那個專案。

| 順序 | 做什麼 | 預期 |
|---|---|---|
| 1 | `powershell ... -File "<專案>\.hybrid\scripts\drive-verify.ps1" -ProjectRoot "<專案>" -Generate` | exit 0，建立基準線 |
| 2 | 再跑一次（不帶 `-Generate`） | exit 0，一致 |
| 3 | 在檔案總管**手動刪掉** `_drive\素材\` 裡一個檔案 | — |
| 4 | 跑 verify | 認得出「遺失」，措辭提到「也可能是還沒同步」 |
| 5 | 改動另一個素材檔的內容 | — |
| 6 | 跑 verify | 分得出「遺失」和「內容變更」是兩件事 |
| 7 | 在 `_drive\衍生品\` 新增、修改、刪除檔案 | — |
| 8 | 跑 verify | **完全沒有反應**（衍生品可重新產生，不追蹤） |
| 9 | `powershell ... -File "<專案>\.hybrid\scripts\drive-restore.ps1" -ProjectRoot "<專案>"` | 給出計畫，**不動手** |
| 10 | 從 Google Drive 版本歷史把檔案救回來，再 verify | 回到一致 |

**失敗怎麼辦**：第 8 步如果衍生品的變動也被報出來 → 記下來。那會在日常使用中不斷製造假警報。

---

# 第 5 階段：判定

## 全部成立才算通過

- [ ] 第 2 階段：**連續** 10 次交接成功
- [ ] 第 3 階段：7 種故障演練，每一種的實際行為與預期一致
- [ ] 第 4 階段：還原演練走完
- [ ] 沒有未解的嚴重問題

## 看到這四種就是不通過

| 形狀 | 為什麼嚴重 |
|---|---|
| **exit 0 但狀態其實不對** | 最危險的失效——你以為成功了 |
| **訊息說的跟實際做的不一致** | 例如說「開工完成」但沒取得租約 |
| **兩台同時持有租約**（兩邊 `lease.json` 都是 `held`）而沒有任何地方指出 | 兩台會同時寫 Drive 上同一份實體 |
| **Drive 上的素材被工具刪掉或覆寫** | 那一層沒有版本歷史，救不回來 |

---

# 回報格式

每一個問題照這個格式，四項都要：

```
做了什麼：（哪一台、哪一步、完整指令）
預期什麼：（SOP 上寫的）
實際看到：（完整輸出，含 exit code）
證據檔案：（3-1-租約-事後.txt 之類）
```

**不要只說「壞了」。** 這條線上好幾個問題是靠「訊息說 X 但檔案顯示 Y」這種對照抓出來的。

---

# 附錄：不用回報的正常行為

以下是設計上刻意如此，看到不用當成問題：

| 現象 | 為什麼 |
|---|---|
| 收工第一次 exit 2 問你 Drive 同步完了沒 | Drive 的完成時點程式測不到，只能問人 |
| `_drive/` 裡的檔案離線時打不開 | 它走 Google Drive 串流。素材要在檔案總管手動設「可離線使用」 |
| 心跳被擋下時工作排程器顯示成功 | 那是為了不讓「正確地拒絕」污染真正的失敗訊號。可見性在 `preflight-skip-log.jsonl` 與開工摘要 |
| 開工摘要多一行「心跳：正常」 | 健康檢查，正常時只印一行 |
| 純中文專案名的專案 ID 開頭是 `project-` | 中文字轉不出英數字，靠後面的 UUID 區分 |
